<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Employee Form</title>
   

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #ff3d00;
            --secondary-color: #2962ff;
            --accent-color: #00e676;
            --dark-bg: #121212;
            --card-bg: rgba(25, 25, 25, 0.8);
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--dark-bg);
            color: white;
            background-image: url('https://cdn.pixabay.com/photo/2017/07/03/20/17/colorful-2468874_1280.jpg'); /* Replace with your preferred background */
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            position: relative;
            overflow-x: hidden;
        }

            body::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: rgba(0, 0, 0, 0.7);
                z-index: -1;
            }

        .container {
            margin-top: 50px;
            margin-bottom: 50px;
            position: relative;
            z-index: 1;
        }

        h2 {
            font-family: 'Poppins', sans-serif;
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 30px;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-shadow: 0 0 10px var(--primary-color), 0 0 20px var(--secondary-color), 0 0 30px var(--accent-color);
            background: linear-gradient(90deg, #ff3d00, #2962ff, #00e676);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            animation: rgbText 3s infinite;
        }

        @@keyframes rgbText {
            0% {
                filter: hue-rotate(0deg);
            }

            100% {
                filter: hue-rotate(360deg);
            }
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-control {
            background-color: rgba(255, 255, 255, 0.1);
            border: none;
            border-radius: 10px;
            color: white;
            padding: 15px;
            font-size: 16px;
            transition: all 0.3s ease;
        }

            .form-control:focus {
                background-color: rgba(255, 255, 255, 0.2);
                box-shadow: 0 0 10px var(--primary-color), 0 0 20px var(--secondary-color), 0 0 30px var(--accent-color);
            }

            .form-control::placeholder {
                color: rgba(255, 255, 255, 0.5);
            }

        .text-danger {
            color: #ff3d00 !important;
            font-size: 14px;
            margin-top: 5px;
        }

        .btn-primary {
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
            border: none;
            color: white;
            padding: 12px 30px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 50px;
            text-transform: uppercase;
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
            z-index: 1;
        }

            .btn-primary::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: linear-gradient(to right, var(--secondary-color), var(--accent-color));
                z-index: -1;
                transition: transform 0.5s;
                transform: scaleX(0);
                transform-origin: 0 50%;
            }

            .btn-primary:hover::before {
                transform: scaleX(1);
            }

            .btn-primary i {
                margin-right: 8px;
            }

        /* Glowing border effect */
        .form-container {
            background-color: var(--card-bg);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 0 20px rgba(255, 61, 0, 0.5), 0 0 40px rgba(41, 98, 255, 0.3), 0 0 60px rgba(0, 230, 118, 0.2);
            backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
        }

            .form-container::before {
                content: '';
                position: absolute;
                top: -50%;
                left: -50%;
                width: 200%;
                height: 200%;
                background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.3), transparent);
                transform: rotate(45deg);
                animation: shine 3s infinite;
            }

        @@keyframes shine {
            0% {
                left: -50%;
            }

            100% {
                left: 150%;
            }
        }

        /* Responsive adjustments */
        @@media (max-width: 767px) {
            h2

        {
            font-size: 2rem;
        }

        .form-control {
            padding: 12px;
        }

        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Employee Form</h2>
        <div class="form-container">
            @model Emp
            <form method="post">
                <div class="form-group">
                    <label for="ename">Name:</label>
                    <input type="text" class="form-control" asp-for="ename" placeholder="Enter name">
                    <span asp-validation-for="ename" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label for="email">Email:</label>
                    <input type="email" class="form-control" asp-for="email" placeholder="Enter email">
                    <span asp-validation-for="email" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label for="salary">Salary:</label>
                    <input type="text" class="form-control" asp-for="salary" placeholder="Enter salary">
                    <span asp-validation-for="salary" class="text-danger"></span>
                </div>
                <input type="submit" class="btn btn-primary" value="Submit">
            </form>
        </div>
    </div>
</body>
</html>